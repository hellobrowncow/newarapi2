
<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <%= image_tag @item.picture.carousel%><br>

    <%= link_to "edit", edit_item_path(@item) %> |  
    <%= link_to "delete", item_path(@item), method: :delete, confirm: "Are you sure you want to delete this item?" %>


    <%= @item.color%>
    <%= @item.clothing_type%>
  </div>
</div>